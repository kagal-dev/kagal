name = "kagal-demo-itty"
main = "src/index.ts"
compatibility_date = "2026-03-12"
compatibility_flags = ["nodejs_compat"]

[dev]
ip = "0.0.0.0"

# Service binding to the DO worker
[[services]]
binding = "KAGAL_WORKER"
service = "kagal-do"
